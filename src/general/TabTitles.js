const TabTitles = (props) => {
  return (
    <div className="mt-4">
      <u>
        <b>
          <h4 className="text-center">{props.title}</h4>
        </b>
      </u>
    </div>
  );
};

export default TabTitles;
